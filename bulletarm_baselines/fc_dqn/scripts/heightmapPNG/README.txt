TestBulletArmCamParams:

a test case uses camera parameters in Bulletarm and Redner(the differentiable renderer)

TestRednerCamParams:

a test case uses suitable camera parameters and Redner

The rest of the Images are heightmap rendered in one iteration

The ones with a white spot in the middle use camera parameters in Bulletarm and Redner 

The ones with a grey rectangle use the original renderer and of course original camera params
